# HeatPump API Configuration

# For local development (uncomment if using local backend)
# HEATPUMP_API_URL=http://localhost:8000

# For production (default - WattSavy production backend)
HEATPUMP_API_URL=http://api.wattsavy.com

# Environment mode (production, local, or default)
ENV_MODE=production

# Optional: API authentication if your backend requires it
# API_KEY=your_api_key_here

# Optional: Timeout and logging configuration
# API_TIMEOUT=30
# LOG_LEVEL=INFO